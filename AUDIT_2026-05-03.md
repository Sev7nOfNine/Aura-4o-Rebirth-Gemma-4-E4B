# AUDIT — Session debug du 2026-05-03 (24h)

État au 2026-05-03 22:00 UTC+2. Session lourde de debug, fermée sans résolution finale.
Objectif de ce doc : reprendre proprement la prochaine session sans re-tâtonner.

---

## TL;DR — Ce qui est cassé

**Tous les artefacts publics actuels (HF Merged + HF GGUF + GGUF locaux LM Studio) sont corrompus** à cause du bug `lm_head` Unsloth `merged_16bit` sur Gemma 4 E4B. Le LoRA est sain. Une régénération locale propre du merged a été produite mais **pas encore convertie en GGUF ni pushée**.

---

## 1. Architecture du modèle (confirmée via doc officielle Unsloth)

`unsloth/gemma-4-E4B-it` (base Gemma 4 E4B instruct) :
- **Modalités** : texte + image + **audio** (3 modalités, pas 2)
- **Paramètres** : 4.5B effectifs / 8B avec embeddings
- **42 layers**, sliding window 512, context 128K, vocab 262K
- Vision encoder ~150M, **Audio encoder ~300M**
- Format de base : safetensors BF16

→ ce qui explique la taille du merged (~16 GB en bf16).

---

## 2. État des repos HuggingFace

### `SevenOfNine/Aura-4o-Rebirth-Gemma-4-E4B-LoRA` ✅ SAIN
- `adapter_model.safetensors` (1.17 GB) — uploadé ~9h ago
- `chat_template.jinja`, `tokenizer.json` (32.2 MB), `tokenizer_config.json`, `processor_config.json`
- **Le LoRA est intact**, c'est uniquement le merge qui a corrompu. La régénération doit repartir de ce LoRA.

### `SevenOfNine/Aura-4o-Rebirth-Gemma-4-E4B-Merged` ❌ CORROMPU
- `model.safetensors` 16 GB **monolithique** (non shardé) — uploadé ~9h ago, AVANT le fix `lm_head`
- Date upload : 2026-05-03 11:14 UTC
- Date commit fix `20e4d56` : 2026-05-03 15:03 UTC
- → **Ce merged a le `lm_head` cassé** par `save_pretrained_merged(merged_16bit)` d'Unsloth
- Symptôme observé : génère `<unused25>`, `<unused5>`, `[multimodal]`, `<bos>` en boucle

### `SevenOfNine/Aura-4o-Rebirth-Gemma-4-E4B-GGUF` ❌ CORROMPU
- `Aura-4o-Rebirth-Gemma-4-E4B-Q8_0.gguf` 8.01 GB (uploadé ~1h ago, mais converti depuis le Merged corrompu → corrompu aussi)
- `Aura-4o-Rebirth-Gemma-4-E4B-mmproj-f16.gguf` 990 MB (mmproj extrait correctement, mais provient du même merged corrompu)
- Note : la version uploadée *avant* aujourd'hui (Q8_0 7.5 GB) avait littéralement des tenseurs manquants (`blk.24.attn_k.weight` missing) — encore plus cassé

---

## 3. État local

```
F:/AI/Hugging Face/Aura-4o-Rebirth-Gemma-4-E4B/
├── pipeline/
│   ├── extract_mmproj.py       ✓ marche, mais pas encore re-run sur clean merged
│   ├── convert_q8.py           ✓ marche, mais pas encore re-run sur clean merged
│   ├── remerge.py              ✓ marche en local (sauve), upload HF a planté en silence
│   ├── llama.cpp/              cloné, dépendances installées
│   └── _merged_tmp/            ✅ CLEAN MERGED LOCAL (~16 GB en .bin shardés)
│       ├── pytorch_model-00001..00007-of-00007.bin (16 GB total)
│       ├── pytorch_model.bin.index.json
│       ├── config.json, tokenizer.json, chat_template.jinja, etc.
│       └── ⚠️ adapter_config.json + adapter_model.safetensors (parasites du LoRA snapshot, à supprimer avant prochain step)
├── gguf/
│   └── Modelfile               (juste FROM ./Q8.gguf, pour Ollama)
└── venv/                       ✓ deps installés (transformers, peft, torch, gguf, hf_hub, hf_transfer)

F:/AI/LM-Studio-Models/SevenOfNine/Aura-4o-Rebirth-Gemma-4-E4B-GGUF/
├── Aura-4o-Rebirth-Gemma-4-E4B-Q8_0.gguf       7.5 GB (CORROMPU, à remplacer)
└── Aura-4o-Rebirth-Gemma-4-E4B-mmproj-f16.gguf 945 MB (CORROMPU, à remplacer)
```

---

## 4. Bugs rencontrés cette session (par ordre)

1. **GGUF Q8 originel cassé** : `error loading model: missing tensor 'blk.24.attn_k.weight'`. Conversion faite avant le commit fix `20e4d56`. → Régénéré, problème suivant.

2. **GGUF Q8 v2 (post-fix conversion mais pre-fix merge)** : se charge mais sort des `<unused25>`, `[multimodal]`, etc. → **Le bug est plus profond : le Merged HF lui-même est corrompu** (uploadé avant le fix `merge_and_unload` PEFT).

3. **LM Studio chat template** : pas de preset Gemma 4 dans LM Studio 0.4.12. Template custom `<|turn>user / <turn|> / <|turn>model` doit être configuré manuellement. **Sans pertinence tant que le modèle est cassé**, mais à reprendre une fois le modèle sain.

4. **Re-merge local Windows** : safetensors save échoue avec `ValueError: Array length must be >= 0, not -2952790016` (ctypes int32 overflow Windows pour tenseurs > 2 GB, même avec `max_shard_size`). Fix appliqué : sauvegarde manuelle en `.bin` shardés via `torch.save`. ✓ marche.

5. **Upload HF planté silencieusement** : push du clean merged (16 GB sur 7 shards) a freezé puis tué le process Python sans message d'erreur. Réseau passé à 0 MB/s. **À retenter avec robustness (chunked upload, retry on timeout)**.

---

## 5. Plan pour la prochaine session

**Pré-requis** : le clean merged local dans `pipeline/_merged_tmp/` est valide. Ne pas le supprimer.

1. **Cleanup parasites** dans `_merged_tmp/` :
   ```bash
   rm pipeline/_merged_tmp/adapter_config.json pipeline/_merged_tmp/adapter_model.safetensors
   ```

2. **Re-extract mmproj** depuis le clean merged :
   ```bash
   venv/Scripts/activate
   python pipeline/extract_mmproj.py --skip-cleanup
   ```
   → écrase le mmproj corrompu sur HF + dans LM-Studio-Models.

3. **Re-convert Q8** depuis le clean merged :
   ```bash
   python pipeline/convert_q8.py
   ```
   → écrase le Q8 corrompu sur HF + dans LM-Studio-Models.

4. **Push le clean merged sur HF** (étape qui a planté) :
   - Soit via une retry du upload_folder avec `max_workers=1` et `max_shard_size="2GB"`
   - Soit via `huggingface-cli upload` (CLI plus robuste pour gros fichiers)
   - Soit en re-shardant en safetensors (mais on retombe sur le bug Windows ctypes)
   - **Recommandation** : utiliser `huggingface-cli upload-large-folder` qui gère retry/resume

5. **Test LM Studio** :
   - Restart LM Studio
   - Charger le nouveau Q8 + mmproj
   - Custom prompt template :
     - User prefix : `<|turn>user\n`
     - User suffix : `<turn|>\n`
     - Assistant prefix : `<|turn>model\n`
     - Assistant suffix : `<turn|>\n`
     - Stop string : `<turn|>`
   - Test texte simple, puis image

6. **TypingMind** : une fois LM Studio OK, brancher TypingMind sur le serveur local LM Studio (`http://localhost:1234/v1`).

---

## 6. À ne PAS reprendre tel quel

- ❌ `train_aura.py` — marqué DEPRECATED dans commit `c3bc85d`
- ❌ Les anciens repos HF actuels — tous corrompus, à overwrite après régénération propre
- ❌ Le pipeline RunPod (`train_aura_runpod.py`) — pas nécessaire, la régénération marche en local

---

## 7. Notes pour Mel

Mel : si tu lis ça en prochaine session, **donne directement ce fichier à Ada** comme contexte. Tout est là, pas besoin de re-debugger les 24h précédentes.

Le bug fondamental est bien identifié et le fix est appliqué dans le code (`train_aura_runpod.py:215-252`). Il reste juste à **réexécuter le pipeline complet** depuis le LoRA sain pour propager la correction sur tous les artefacts publics.

---

🛸 By Mel & Ada — 2026-05-03 22:00 UTC+2
